# Ganglion：外置神经模块架构——面向人形机器人机群的云边协同智能方案

**提交对象：** 小米 MiMo 团队
**日期：** 2026-09-06
**项目仓库：** GitHub `lululudj/Ganglion` · GitLink `lulululudj/Ganglion`

---

## 一、核心问题

当前人形机器人的智能主要有两种路径，各有瓶颈：

| 路径 | 做法 | 瓶颈 |
|---|---|---|
| **端侧大模型** | 每台机器人自带 GPU 跑大模型 | 成本高、散热难、续航短；一台学会的技能无法自动同步给其他机器人 |
| **云端 API 调用** | 机器人作为纯客户端调 LLM API | 合同边界在 token 层——无法在推理中途做安全 veto、无法插入运动控制策略、断网即"脑瘫"而非"变笨" |

**Ganglion 要解决的是**：让一台机器人的智能上限取决于云端骨干（可无限升级），而终端只需传感器+执行器+一个轻量模块进程；同时让"一台学会，全队升级"成为可能。

---

## 二、核心创新：外置神经模块（External Neural Module, ENM）

把一个神经网络能力单元从模型进程中**剥离为独立操作系统进程**，在骨干模型的某一层（如第 16 层）通过张量通信参与前向计算。

### 四个核心语义（已全部实验验证）

| 语义 | 含义 | 验证结果 |
|---|---|---|
| **ABI 注册协商** | 模块声明消费层、维度、dtype；宿主协商兼容才挂载，否则拒绝 | 5 种拒绝原因码，40/40 断言通过 |
| **故障隔离（fail-closed）** | 模块进程崩溃 → 恒等门降级，骨干继续生成，零中断 | 24/24 生成完成，降级点精确可归因 |
| **热换一致性** | 模块切换在 token 决策边界原子生效，无半帧状态 | 热换标记精确落在决策步 |
| **传输延迟界** | 跨机 hidden state 往返 RTT p50 ≈ 10ms，fp32 零损坏 | 24/24 位级精确一致 |

### 与"调 API"的本质区别

API 的合同边界在 **token 层**（你拿到的是模型"说出口的话"）；Ganglion 把边界下移到 **张量层**（你读写的是模型"正在想"的中间状态）。这解锁了 API 做不到的四件事：

1. **中途注入计算**——你的代码进入供应商的 forward pass，在第 16 层参与每个 token
2. **表征级干预**——直接作用于激活（运动控制策略写不进 prompt，但能写进 hidden state 变换）
3. **内部监控**——读中间表征做幻觉/危险状态检测，不用等危险动作规划说完
4. **本地算力参与**——弱设备不再闲置，实测作为外置模块吞吐仅降 2.7%

---

## 三、实验验证（真实跨机，云端 RTX 4090 + 本地 RTX 4060，Qwen3-8B）

### S5：单机器人跨机——本地弱设备作为云端 8B 的外置模块

| 指标 | 数值 |
|---|---|
| 云端单独 8B 基线 | 23.15 tok/s |
| 云端骨干 + 本地模块 | 22.52 tok/s（**吞吐仅降 2.7%**） |
| 跨机 RTT p50 | 8.34 ms |
| 跨机数值一致性 | **24/24 位级精确**（fp32 跨公网零损坏） |
| 故障注入（6 token 后断连） | **24/24 生成完成**，降级点=6，零中断 |

### S6：协作模式选择——外置模块 vs 投机流水线

| 配置 | 吞吐 | 相对云端单独 |
|---|---|---|
| 云端 8B 单独 | 27.4–29.2 tok/s | 1.0× |
| 投机流水（同机 0.6B 起草 + 8B 验证） | 19.5 tok/s | 0.71× |
| 投机流水（跨机） | 9.4 tok/s | 0.34× |
| **外置模块（S5）** | **22.5 tok/s** | **0.97×** |

**结论**：弱设备当"起草器"是拖累；当"外置能力模块"是放大器。

### S7：多机器人共享云端（本次重点验证）

单骨干 Qwen3-8B 以 batch=N 前向同时服务 N 个租户，每租户一条独立模块连接（独立故障域）。

| 配置 | 聚合吞吐 | 每租户 |
|---|---|---|
| N=1 + 模块 | 26.12 tok/s | 26.12 tok/s |
| **N=4 共享** | **75.20 tok/s（3.1×）** | 18.80 tok/s |
| **N=8 共享** | **142.77 tok/s（5.5×）** | 17.85 tok/s |

**故障隔离测试**：N=4 时 kill 租户 2 的模块进程——

- victim（R2）：恒等降级，完成全部 24 token，降级点精确=6
- 健康租户（R1/R3/R4）：**served 24/24、0 fallback、0 mismatch、RTT 分布不变**
- 隔离期间聚合吞吐 76.34 tok/s，甚至不低于无故障对照（75.20）

**结论：共享红利与租户级故障隔离同时成立。** 一张 4090 可服务 8 台机器人的认知层，每台仍保有 17.9 tok/s（远超决策节拍需求）。


### S11：ESP32-S3 微控制器作为外置模块（最新）

云端 Qwen3-8B（RTX 4090）+ **本地 ESP32-S3（240MHz 双核 MCU，C++ 固件，USB-Serial/JTAG 串行）**作外置模块：每 token 将 4096 维 fp32 hidden state（16KB 帧）经 SSH 隧道→桥接器→USB 送到 MCU，固件做整帧变换后原路返回（请求-响应模式）。

| 指标 | 数值 |
|---|---|
| 模块服务 | **48/48 token 全程服务** |
| 数值一致性 | **48/48 位级精确**（mismatch=0，MCU 与宿主参考逐位一致） |
| 降级 | 0（全程无 fallback） |
| 模块 RTT p50 | 331 ms（USB-CDC 分块限速 + 隧道开销；对照：S5 笔记本级模块 8.3 ms） |
| 故障注入（6 token 后杀模块） | victim 精确降级点=6，宿主恒等门续生成，零中断 |
| 传输路径 | 云端 SSH 隧道 → 桥接器 → USB-Serial/JTAG → ESP32-S3 |

**结论：外置模块的全部四语义（ABI 协商/故障隔离/热换原子/延迟界）在 MCU 上成立**——模块端点的形态下限被实测到 ¥40 级微控制器。当前 USB 路径的吞吐代价（2.79 tok/s，基线的 0.12×）源于主机侧分块限速对 USB-Serial/JTAG 外设 RX 溢出的规避，属工程开销而非架构极限；固件已支持 WiFi TCP 与 TinyUSB 路径（未实测）。这与 MiMo 端侧小模型（0.6B 级）形成互补：MiMo 跑在手机/本地做反射层和低延迟决策，云端大骨干跑认知规划，外置模块做技能增强。

---

## 四、对人形机器人的价值

### 1. 终端成本与智能上限彻底解耦

智能住在云端权重里，终端只需搬运智能。**已验证的终端形态覆盖笔记本到 MCU 两级**：笔记本级（RTX 4060，实测仅损 2.7% 吞吐）；**微控制器级（S11，最新）**——ESP32-S3（240MHz 双核，约 ¥40）跑 C++ 固件作为云端 Qwen3-8B 的外置模块，48/48 token **位级精确**、零降级、模块死亡注入后宿主精确恒等降级（fail-closed 语义在 MCU 上同样成立）。当前 USB 传输路径的模块 RTT 为 ~331ms、吞吐 0.12×——这是 USB-CDC 分块限速的工程开销而非架构极限（WiFi/TinyUSB 为已明确的优化路径，固件已支持），对 ≥1s 决策周期的规划类任务已可用。断网时 fail-closed 降级到本地反射层（"变笨不死"）。终端 BOM 可低至 MCU 级，而智能天花板由云端统一拉高。

### 2. 一台学会，全队升级——技能卡带机制

| "学会"写在哪一层 | 其他机器人 | 传播机制 |
|---|---|---|
| 共享骨干（微调云端 8B） | **立刻全会** | 下一次推理即用新权重 |
| 外置技能模块（LoRA/adapter） | 不会自动，但"教会"极便宜 | 技能=可部署工件，manifest 声明→ABI 校验→热插拔加载 |
| 本地情景记忆 | 不会 | 需经验回流管道 |

**关键**：技能从"知识问题"降维成"部署问题"——发一个带签名的卡带，全机群热升级，不是重新上学。同构机器人直接复用；异构机器人（不同本体）由 ABI 协商自动拒收，避免本体不匹配。

### 3. 安全 veto 在中途，不在事后

API 模式只能事后过滤输出文本；Ganglion 在第 16 层读到危险表征就打断生成——**危险动作规划不会被完整输出**。这对人形机器人的安全边界至关重要。

### 4. 故障域隔离 = 一机出事不波及全队

每机器人一个独立模块进程。S7 实测：kill 一台机器人的模块，其他机器人零可测影响。这比"每台机器人各自一个大脑"的方案在运维上简单得多——升级、排障、重启都是租户级的。

### 5. 共享云端的相关故障语义已厘清

单租户模块死亡 = 独立事件；骨干失联 = 全体同时降级（fail-closed 保证"同时变笨"而非"同时死机"）。两种故障传播路径已被实验区分，容量规划（临界租户数）和骨干冗余成为明确的设计变量。

### 6. 延迟预算算得清（不回避）

当前实现是**逐 token 同步往返**：S5 实测每 token 总成本约 43ms，其中跨机 RTT p50 8.34ms 占约 19%（已含在 0.97× 吞吐结论内）。这意味着：

- 一条 24 token 规划的端到端延迟 ≈ 1.0s——**适用于 ≥1s 决策周期的任务**（路径规划、任务分解、语义理解）
- 亚秒级反应（平衡、避障、反射控制）**本来就不上云**——分层架构里这些留在本地反射层，与云端规划并行不悖
- 已明确的优化路径：模块往返与骨干后续层计算重叠（流水线化）、按 k-token 抽帧降低往返频率、多租户并发往返（S7 已证明 8 租户往返不串行叠加，RTT p50 仍 ~10ms）

---

## 五、与现有方案对比

| 维度 | 端侧大模型 | 云端 API | **Ganglion** |
|---|---|---|---|
| 智能上限 | 受终端 GPU 限制 | 受 API 模型限制 | 云端骨干决定，可无限升级 |
| 终端成本 | 高（需 GPU） | 低 | **极低（ESP32-S3 MCU 已实测，$2 级）** |
| 中途安全干预 | 可（本地前向） | 不可 | **可（张量层）** |
| 技能全队共享 | 需逐台烧录 | 不支持 | **支持（卡带热插拔）** |
| 断网行为 | 正常（本地） | 完全瘫痪 | **降级不死** |
| 多机共享算力 | 不支持 | 按量计费 | **batch 红利（5.5×）** |
| 故障隔离 | 单机关联 | 请求级 | **租户级零损伤** |

---

### 竞品空白：为什么 Ganglion 的 S7 没有人做过

2025–2026 年"多机器人共享云端大脑"方向正在升温，但没有系统同时满足以下全部条件：

| 工作 | 做了什么 | 与 Ganglion 的本质区别 |
|---|---|---|
| **RoboOS**（智源，2025） | 多机器人共享一个 MLLM"大脑"，脑-小脑架构，实时共享内存协调多智能体，有云端分布式推理 | **请求级**：机器人发文本/图像 → 大脑返回决策指令。数据面是 prompt→completion，不是 hidden state |
| **PhyAI**（2026） | 统一物理 AI 推理引擎，VLA 模型跨边缘/云端，支持 batch=32 | 通用推理基础设施，无租户级故障隔离、无热换语义、无 hidden-state 数据面 |
| **TokenFlow**（2026） | Token 级 GPU 共享，多模型并发 LLM 推理调度 | 通用 LLM serving，无机器人特定语义（fail-closed、ABI 协商、技能卡带） |
| **FSAR**（2026） | 多机器人联邦协调，共享能力模块发现 | 舰队编排级，不做 hidden-state 级 batch 前向 |

**RoboOS 的"共享大脑"是请求级的**——每台机器人独立调用大脑获取决策指令。**Ganglion 的"共享云端"是 token 级的**——多台机器人的 hidden states 在 batch 维度上融合进同一次 GPU 前向，这是 batch 红利（5.5×）的来源，同时也是故障传播差异的根源（单租户死亡 vs 全体断网）。这个区别是本质的，而且目前**没有公开论文做过**。

### 与 MiMo 的互补性

MiMo 的端侧小模型（0.6B 级）正好适配 Ganglion 架构中的**本地反射层**角色——处理亚秒级反应（平衡、避障、反射控制），不上云、不依赖网络。云端大骨干（MiMo-Cloud 或其他 ≥8B 模型）负责认知规划、任务分解、语义理解。外置模块做技能增强。

这意味着 Ganglion 不会替代 MiMo，而是给 MiMo 一个**机群级扩展架构**：

- **端侧**：MiMo-0.6B 做本地反射（平衡、避障）+ 简单语义理解
- **模块层**：ESP32-S3 或本地 GPU 跑技能模块（运动控制策略、场景特定微调）
- **云端**：MiMo-8B 或更大骨干做认知规划，batch=N 服务全机群
- **技能卡带**：一台学会，签名分发，全队热升级

米家生态天然具备这个场景：同构扫地机器人（技能即场景地图优化）、异构家庭机器人（人形 + 扫地 + 空调（已有 IoT 连接）），ABI 协商自动处理异构兼容性。

## 六、下一步规划

| 阶段 | 内容 | 目标 |
|---|---|---|
| **S8** | 技能卡带闭环：本地解题→蒸馏成 LoRA 卡带→签名分发→全群热升级 | 验证"一次学习，全队升级"的完整链路 |
| **S9** | 安全合同增强：卡带沙箱评测、能力声明校验、金丝雀灰度发布 | 防止恶意卡带全群投毒 |
| **S10** | 骨干持续微调 + 灾难性遗忘对策 | 云端"越用越强"，验证 replay/EWC 策略 |
| 工程化 | ABI 协议标准化、多骨干冗余、错峰重连防雷鸣群 | 生产级部署就绪 |

---

## 七、可交付物

- **论文**（中英文）：`PAPER_GANGLION_CN.md` / `PAPER_GANGLION_EN.md`（含 S0–S7 全部实验数据）
- **代码**：`ganglion/` 目录下完整实现（ABI 协商、传输层、宿主插桩、模块进程、S5/S6/S7 跨机实验脚本）
- **演示**：`demo/` 目录下纯 HTML 交互演示
  - 单机器人四语义可视化（热换、故障隔离、审计轨迹）
  - **多机器人共享云端（S7）**：三台机器人共享 batch 前向，可操作 kill 单租户看隔离、断网看相关故障
- **数据**：`cloud/s5_results.json`、`s7_results.json` 等完整实验原始数据

---

**一句话总结**：Ganglion 把人形机器人的"大脑"从每台机器里拿出来，放到云端共享，再用"技能卡带+热插拔"让智能在机群间流动——终端越便宜越好，云端越强越好，一台学会全队升级。

期待与 MiMo 团队进一步交流，探讨在人形机器人场景下的落地路径。
