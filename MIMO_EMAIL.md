主题：【合作交流】多机器人共享云端大脑——Ganglion 外置神经模块架构（真实跨机 + MCU 实测）

MiMo 团队各位老师好：

我是 Ganglion 项目的独立研究者。近期完成了一套面向机器人机群的云边协同智能架构验证，认为与 MiMo 在端侧模型 + 机器人方向的工作有很强的互补性，特此致信。

一句话概括：把机器人的"大脑"从每台机器里拿出来，放到云端共享；终端只保留传感器、执行器和一个轻量模块进程。技能以"卡带"形式热插拔分发——一台学会，全队升级。

（一）和"调 API"的本质区别

API 的合同边界在 token 层（拿到的是模型"说出口的话"）；Ganglion 把边界下移到张量层（读写的是模型"正在想"的中间状态）。这解锁四件事：

1. 中途注入计算——代码进入 forward pass，在第 16 层参与每个 token
2. 表征级干预——运动控制策略写不进 prompt，但能写进 hidden state 变换
3. 内部监控——读中间表征做危险检测，不用等动作规划完整输出
4. 本地算力参与——弱设备不再闲置

（二）关键实验数据（真实硬件，不是模拟）

1. 单机跨机（S5）：云端 RTX 4090 跑 Qwen3-8B，本地 RTX 4060 经 SSH 隧道跨公网作外置模块。吞吐仅降 2.7%（22.5 vs 23.2 tok/s），fp32 跨网 24/24 位级精确，断连后宿主零中断降级。

2. 多机共享（S7）：单骨干 batch=N 同时服务 8 租户。聚合吞吐 142.8 tok/s（5.5 倍），每台仍保有 17.9 tok/s。kill 任一租户的模块，其余租户 0 fallback / 0 mismatch / 24/24 正常服务——共享红利与租户级故障隔离同时成立。

3. **MCU 级终端（S11，最新）**：ESP32-S3（240MHz 双核，几块钱的 MCU）跑 C++ 固件作外置模块——48/48 token 位级精确、零降级、kill 注入后宿主精确恒等降级（fail-closed 语义在 MCU 上成立）；当前 USB 路径模块 RTT ~331ms（工程开销，WiFi 路径待测），对 ≥1s 决策周期已可用

（三）为什么没人做过

智源 2025 年的 RoboOS 是目前最接近的多机器人共享大脑方案，但它在请求级运作——机器人发文本，大脑返回决策指令。

Ganglion 在张量级运作——多台机器人的 hidden states 在 batch 维度融合进同一次 GPU 前向。这是 5.5 倍聚合吞吐的来源，也是租户级故障隔离的前提。这个组合目前没有公开论文做过。

（四）与 MiMo 的互补

Ganglion 不替代 MiMo，而是给 MiMo 一个机群级扩展架构：

- 端侧：MiMo-0.6B 做本地反射（平衡、避障），不上云
- 模块层：ESP32 或本地 GPU 跑技能模块（运动控制策略、场景微调）
- 云端：MiMo-8B 或更大骨干做认知规划，batch=N 服务全机群
- 技能卡带：签名分发，全队热升级，ABI 协商自动处理异构兼容

米家生态天然适配——同构扫地机器人（技能=场景地图优化）、异构家庭机器人（人形 + 扫地 + IoT）。一台学会，全队升级。

（五）交流意向

我们有完整的论文（中英文）、全部实验代码与数据、可交互 HTML 演示（单机器人四语义 + 多机器人共享云端，可现场操作 kill 租户看隔离、断网看降级）。

如果各位老师有兴趣，可以：

1. 约一次线上或线下技术分享（30 分钟，现场演示）
2. 提供论文全文 + 代码仓库 + 原始实验数据
3. 探讨在人形机器人具体机型上的联合验证方案

期待回复。

此致
敬礼

Ganglion 项目组
2026 年 9 月 6 日

项目仓库：
GitHub: https://github.com/lululudj/Ganglion
GitLink: https://www.gitlink.org.cn/lulululudj/Ganglion

---

【微信精简版，可直接粘贴】

MiMo 团队老师们好，我做一个叫 Ganglion 的架构：把机器人大脑放云端共享，终端只留传感器 + 轻量模块进程，在骨干模型中间层（张量级，不是 API 的 token 级）参与每个 token 的前向计算。真实跨机验证：8 台机器人共享一张 4090（Qwen3-8B），聚合吞吐 5.5 倍，kill 任何一台的模块其他台零影响；断网全体 fail-closed 降级（变笨不死）；ESP32 几块钱的 MCU 实测也能当外置模块（48/48 token 位级精确，四语义全过）。比请求级共享大脑多一层：张量级 batch 融合 + 租户级故障隔离。技能可蒸馏成"卡带"热插拔分发——一台学会拿被子，全队热升级。有完整论文、代码和可交互 demo，想约个时间给老师们现场演示，看是否有合作空间。
